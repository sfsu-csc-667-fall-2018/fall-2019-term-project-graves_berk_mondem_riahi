extends layout

block content
  h1 Basic Chat App
      
      ul(id = "messages")

      form(method='POST' action='/lobby' )
        input(id = "m" autocomplete = "off" maxlength="50")
        button Send


  script( src="https://cdn.socket.io/socket.io-1.2.0.js")
  script( src="https://code.jquery.com/jquery-1.11.1.js")
  script(src='/javascripts/chatPost.js')

 

     